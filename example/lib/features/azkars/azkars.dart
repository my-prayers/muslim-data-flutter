import 'package:flutter/material.dart';

class Azkars extends StatelessWidget {
  const Azkars({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Azkars')),
      body: Center(child: Text('Azkars')),
    );
  }
}
